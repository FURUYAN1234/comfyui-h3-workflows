# v1.2.0-ref1 検証範囲（2026-09-26）

- 参照画像の番号順・バッチ全件・不足検出・再開指紋を焦点テストで確認。
- 修正元の通常経路では参照画像2枚、3秒、72フレームの実生成を確認。この配布用ソースからの再生成は未実施。
- T2V・I2V・Ref2V・MVのワークフローJSON設定、同梱依存、展開ZIP、タグ再構築を確認。
- 長尺MVや音声の全編視聴、別PC GPU実行は未確認。

# v1.2.0 検証範囲（2026-09-22）

v1.1.9のT2V・I2V・Ref2V機能を保持し、YuE2等の可搬MV素材を受ける新しい通常経路を追加しました。

- 公開MV JSONをComfyUIへ読み込み、26ノード・35リンク、不足ノードなし、レイアウトと説明枠を確認。
- `mv-assets/...` の相対指定、標準ComfyUI `IMAGE`、音源1秒切出し、字幕OFFの入力経路を実ノードで実行。
- MVコア8件、可搬性3件、H3 Long Video 186件、H3 Standard Prompt 76件、ルート回帰3件に合格。
- 以前の通常ワークフロー実行で20.000秒、864×480、24fps、H.264/AAC、字幕4区間の `final.mp4` を生成。元音源との相関0.999712、タイトル／終了リンク、映像音声フェードを確認。
- キャラクターは参照画像へ近づきましたが、完全な顔一致は保証しません。画風・顔・髪・服装の目視確認を前提とします。

## 継承したv1.1.9検証

v1.1.8を基準に、指定尺を超える明示タイムラインと、混合禁止リスト内の `歌` を見落とす日本語指示解析を修正しました。v1.1.8のLM Studio進捗表示、モデル、映像4step、音声2step・denoise 0.5、可変尺、区間再開は維持しています。

- 異常例：5秒要求に対する `[00:00-00:45]` を拒否し、5秒内の正常案を受理。
- 正常例：引用符内の時刻や、要求尺内のタイムラインは尺超過として誤検出しないことを確認。
- 異常例：`台詞・歌・音楽はなし` を全体無言として認識。
- 正常例：`歌はなし。女性が「こんにちは」と話す。` は指定台詞まで抑止しないことを確認。
- T2V、I2V、Ref2Vを修正後の通常ワークフローから内蔵ブラウザーで実生成。各5.000秒、864×480、24fps、H.264/AACで、区間監査は合格。
- 実環境Standard Prompt 65件、配布用Standard Prompt／デバイス58件、Long Video 88件、配布・設定11件の回帰検査に合格。
- 新規展開物の `verify_package.py`、全ハッシュ、余分なファイル、クリーンタグからの再構築はv1.2.0公開前検査で実行します。

生成映像では人物の分身、明らかな髪型不一致、台詞破綻、二重発声を確認していません。ローカルの台詞検査は合格しましたが、人による全編試聴まで確認済みとはしていません。別PC・WindowsネイティブGPU実行は未検証です。
