# v1.1.2 — 2026-09-09

## Included change

- A 16–20 second request is planned as one H3 pass. Requests above 20 seconds continue with segments.
- LM conversion now rejects a timed action that crosses a generation boundary, carries only ongoing ambience into a later segment, and supplies `[Japanese]` for unmarked Japanese dialogue.
- The source ZIP builder fixes its `ZipInfo.create_system` value to Unix (`3`), making the reviewed Windows source rebuild byte-identical to the release ZIP.

## Validation boundary

- The package verifier, workflow configuration checks, source syntax checks, manifest, and exact v1.1.2 ZIP rebuild passed.
- The supplied validation record includes a Ref2V 20-second runtime report. A new all-mode GPU run and other-PC runtime run were not performed for this publication.
